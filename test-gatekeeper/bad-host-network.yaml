apiVersion: v1
kind: Pod
metadata:
  name: bad-host-network
spec:
  hostNetwork: true
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
  containers:
  - name: nginx
    image: nginx:1.25.1
    resources:
      limits:
        cpu: "500m"
        memory: "512Mi"
